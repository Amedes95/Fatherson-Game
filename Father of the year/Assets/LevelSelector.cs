using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class LevelSelector : MonoBehaviour
{
    public GameObject WorldLevels;
    public float ShiftDistance; // should be 10


    //// Update is called once per frame
    //void Update()
    //{

    //    // Translates levels within worlds
    //    if (Input.GetKeyDown(KeyCode.A) && LevelManager.StartOfList == false)
    //    {
    //        WorldLevels.transform.Translate(Vector3.right * ShiftDistance);
    //    }
    //    if (Input.GetKeyDown(KeyCode.D) && LevelManager.EndOfList == false)
    //    {
    //        WorldLevels.transform.Translate(Vector3.left * ShiftDistance);
    //    }

    //    // Translates worlds
    //    if (Input.GetKeyDown(KeyCode.W))
    //    {

    //    }
    //}
}
